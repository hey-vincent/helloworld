#include <iostream>
#include <string>
using namespace std;

// intercept a string-like input as a given criterion
vector<string> Intercept(string& strInput, string& signal)
{
    if(strInput.size()==0)
    {
        return 0;
    }
    else
    {
        //find(strInput,)
    }
}
